/**
 * @file useAuthSession.ts
 * @deprecated Session bootstrap moved to AuthProvider. Kept for backward-compatible imports.
 */
export { useAuthContext as useAuthSession } from '@/providers/AuthProvider';
