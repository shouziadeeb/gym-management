/** Expo route: /auth/forgot-password — password reset email request. */
import { ForgotPasswordScreen } from '@/screens/auth/ForgotPasswordScreen';

export default function ForgotPasswordRoute() {
  return <ForgotPasswordScreen />;
}
