import { GymJoinScreen } from '@/screens/join/GymJoinScreen';

/** Deep link landing: gymos://join/{slug} or {WEB_ORIGIN}/join/{slug} */
export default function JoinGymRoute() {
  return <GymJoinScreen />;
}
